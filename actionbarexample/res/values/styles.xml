<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
    Example of how one could override the default style for the ActionBarItem.
    The style below is used in conjunction with the actionBar.setHomeAction
    that's comment out in the HomeActivity. 
    -->
    <!--
    <style name="ActionBarItem">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingLeft">5dp</item>
        <item name="android:paddingRight">5dp</item>
        <item name="android:layout_margin">0px</item>
    </style>
     -->

    <!--
    Example of how to change the style of the progress bar.
    -->
    <!--
    <style name="ActionBarProgressBar" parent="@android:style/Widget.ProgressBar.Small.Inverse">
    </style>
     -->

    <!-- Demonstrates how we can style the action bar differently for another
         activity. Drawables for the tabs used in this examples has been stolen
         from the iosched app <http://code.google.com/p/iosched>.
         
         This example also demonstrates that there's no dependency on the
         themes defined in the action bar as long as all the styles has been
         setup to point to a style resource. -->
    <style name="OtherTheme" parent="android:style/Theme.Light">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- These are the styles that is needed for tabs. -->
        <item name="actionBarTabBarStyle">@style/FancyTabBar</item>
        <item name="actionBarTabViewStyle">@style/FancyTabView</item>
        <item name="actionBarTabTextStyle">@style/FancyTabText</item>
        <item name="actionBarStyle">@style/BlueActionBar</item>
        <item name="actionBarItemStyle">@style/BlueActionBarItem</item>
        <item name="actionBarItemDivider">@color/blue_actionbar_divider</item>
        <!-- These are the resources needed by the action bar, they all refer
             to default values. -->
        <item name="actionBarTitleTextStyle">@style/ActionBar_TitleText</item>
        <item name="actionBarSubtitleTextStyle">@style/ActionBar_SubtitleText</item>
        <item name="actionBarHomeItemStyle">@style/ActionBar_HomeItem</item>
        <item name="actionBarHomeLogoStyle">@style/ActionBar_HomeLogo</item>
        <item name="actionBarProgressBarStyle">@style/ActionBar_ProgressBar</item>
    </style>

    <style name="BlueActionBar" parent="@style/ActionBar_Container">
        <item name="android:background">@drawable/blue_actionbar_background</item>
    </style>
    <style name="BlueActionBarItem" parent="@style/ActionBar_Item">
        <item name="android:background">@drawable/blue_actionbar_item_btn</item>
    </style>
 
    <style name="FancyTabBar" parent="ActionBar_TabBar">
        <item name="android:background">@drawable/tab_custom_off_background</item>
        <item name="android:paddingTop">5dip</item>
        <item name="android:layout_height">38dp</item>
    </style>
    <style name="FancyTabView" parent="ActionBar_TabView">
        <item name="android:background">@drawable/tab_custom_indicator</item>
        <item name="android:layout_height">38dp</item>
        <item name="android:layout_marginLeft">2dip</item>
        <item name="android:layout_marginRight">2dip</item>
    </style>
    <style name="FancyTabText" parent="ActionBar_TabText">
        <item name="android:textColor">?android:attr/textColorPrimary</item>
        <item name="android:textSize">14sp</item>
    </style>
</resources>